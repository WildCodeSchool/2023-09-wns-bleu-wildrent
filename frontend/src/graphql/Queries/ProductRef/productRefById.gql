query ProductRefById($productRefId: Int!) {
  productRefById(productRefId: $productRefId) {
    id
    name
    description
    image
    priceHT
    quantity
    quantityAvailable
    subCategory {
      id
      name
      category {
        id
        name
      }
    }
  }
}
